#include "main.h"

/**
 *mul - function that print the multiple of two number
 *@x: the first number
 *@y: the second number
 *Return: the multiple value
 */

int mul(int x, int y)
{
	return (x * y);
}
